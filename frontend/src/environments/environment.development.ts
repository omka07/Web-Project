// Development defaults. Angular CLI swaps this in for `environment.ts`
// whenever the development configuration is active (which is the default
// for `ng serve`). The file swap is wired in `angular.json`.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api',
};
